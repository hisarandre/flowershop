CREATE TABLE app_user (
    id UUID PRIMARY KEY,
    last_name VARCHAR(255),
    first_name VARCHAR(255),
    gender VARCHAR(255),
    email VARCHAR(255),
    password VARCHAR(255),
    role VARCHAR(255)
);